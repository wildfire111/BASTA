[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    slow: long-running tests
    integration: integration tests
    regression: regression tests
filterwarnings =
    ignore:.*gaussian_filter1d.*:DeprecationWarning
